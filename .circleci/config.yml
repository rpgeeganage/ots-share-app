version: 2.1

jobs:
  validate:
    steps:
      - checkout
      - run:
        name: Run test
        command: |
          docker-compose -p e2e -f ./docker-compose.e2e.yml up e2e

workflows:
  validate-workflow:
    jobs:
      - validate